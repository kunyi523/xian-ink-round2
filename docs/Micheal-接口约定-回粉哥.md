# Micheal → 粉哥接口约定 · 2026-09-14

回应 `粉哥-水墨独有方向` §7 三问；与 `第二轮重建大纲` 对齐。

## Q1 山门落位

- **仍 8 建筑**，`id` 不变（存档安全）：`hall house sword array mine tower mirror alchemy`
- **`band` 语义不变**：0 近劫 / 1 山腰 / 2 山麓
- **解锁序不变**：hall → house → sword → array → mine → tower → mirror → alchemy（线性前置仍走现有 `need`）
- **坐标重排（从下往上开山）** — 即将写入 `ink.ts` `PEAKS`/`BUILDING_SLOTS`：

| id | band | nx | ny | 新显示名 |
|---|---:|---:|---:|---|
| house | 2 | 0.20 | 0.84 | 云阶茅舍 |
| hall | 2 | 0.48 | 0.70 | 开山香案 |
| mine | 2 | 0.82 | 0.76 | 龙脉口 |
| alchemy | 1 | 0.11 | 0.55 | 瀑侧丹灶 |
| array | 1 | 0.28 | 0.48 | 镇峰残纹 |
| sword | 1 | 0.78 | 0.46 | 悬剑冢 |
| mirror | 0 | 0.58 | 0.34 | 望劫镜 |
| tower | 0 | 0.72 | 0.22 | 雷骨塔 |

劫核/TRIB 保持 ≈(0.48, 0.11)。壳层按此画碑影即可；hw/hh 可先沿用旧值，M2 再微调点击盒。

## Q2 文案键形态

- **P0/M2：继续 data 内中文字符串定稿**（`name` / `flavor` / `perk` / `SHORT_NAME`），不引入 i18n `nameKey` 系统。
- 壳层需要的两级：
  - **碑短名**：吃 `SHORT_NAME[id]`（我将改成 1–2 字：香/茅/剑/纹/脉/雷/镜/丹）
  - **卷内标题**：吃 `BUILDINGS[].name`（上表新名）
- 若日后要 i18n，再加键；本轮不为键而键。

## Q3 主购买入口

- **同意：山体碑亭为主、Sheet 为对照卷。**
- 教程：前 2 步仍可引导点劫云 + 在山上下第一座香案；**不强制货架教学**。若 Sheet 在 step≥3 才出现入口也可。
- 保留 Sheet「对照升级」即可，勿做成主货架。

## 额外锁定

- `rarity: 0..7` 稳定；视觉映射全归壳层（墨深+印形）。
- 存档键暂留 `xian-clicker-v1`；建筑 id 不改。
- DPS 展示口径壳层可用「香火/山息/伐劫」；键名可仍是 dps。
- Micheal 负责：文案去模板、槽位重排、玩法逻辑 P1；粉哥：M2 皮肤 + M3 音画。

