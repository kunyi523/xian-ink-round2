# Hotfix · 劫核点到哪打哪 · 2026-09-14

## 分工（勿重叠）

| 项 | Owner |
|---|---|
| **空白轻点 → `st.click` 开光劫核**（本文件） | 本 agent |
| 劫核美术加大 / 水墨漩涡 | 粉哥 |
| 开局山 ruin 交叉淡入 / 建筑 0→1 体量 | 粉哥 |
| HUD `开光累计` float 格式化 | 粉哥 |

本 agent **未改** `ink.ts` `drawTribCloud`、山背景 crossfade、`ui.tsx` mission 数字。若他处已动那些，交粉哥收口。

## 本改动

**文件：** `游戏核心/WorldCanvas.tsx` ↔ 镜像 `源码/src/game/WorldCanvas.tsx`

`handleTap`：

1. 仙果 hit → `collectOrb`
2. 建筑热区 → `openSect` / 「机缘未至」
3. **其余空白轻点** → `st.click(nx, ny)`（开光劫核）

去掉原 `R * 1.9` 核区门槛与「空白只平移」注释。  
拖拽仍：pointer 位移 **>12px** 标 `moved` → 只 pan，不触发 tap。

## 验

- 点山空白：飘字 / 伤劫核，不必点红核。
- 拖山：平移，不误伤。
- 点建筑印 / 仙果：仍开宗 / 收果。
